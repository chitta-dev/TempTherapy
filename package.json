{
  "name": "therapy-platform-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "concurrently -n \"BACKEND,WEB\" -c \"blue.bold,green.bold\" \"npm run start:backend\" \"npm run start:web\"",
    "start:backend": "npm --prefix apps/backend run dev",
    "start:web": "npm --prefix apps/web-admin run dev",
    "start:mobile": "npm --prefix apps/mobile start",
    "dev:all": "concurrently -n \"BACKEND,WEB,MOBILE\" -c \"blue.bold,green.bold,magenta.bold\" \"npm run start:backend\" \"npm run start:web\" \"npm run start:mobile\"",
    "build:all": "npm --prefix packages/shared run build && npm --prefix apps/backend run build && npm --prefix apps/web-admin run build"
  },
  "devDependencies": {
    "babel-preset-expo": "~57.0.11",
    "concurrently": "^8.2.2"
  }
}
